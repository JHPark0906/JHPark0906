#include <iostream>

#define STR_MAX 21

using namespace std;

int main() {
    char name[STR_MAX];
    cin >> name;
    cout << ":fan::fan::fan:\n:fan::" << name << "::fan:\n:fan::fan::fan:";
    return 0;
}